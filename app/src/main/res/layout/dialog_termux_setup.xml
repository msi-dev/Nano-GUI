<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#000000"
    android:padding="20dp">

    <!-- Title -->
    <TextView
        android:id="@+id/setup_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Termux Nano Setup"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        android:fontFamily="monospace"
        android:paddingBottom="12dp" />

    <!-- Description -->
    <TextView
        android:id="@+id/setup_desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Run this command in Termux to replace standard nano with a GUI wrapper that opens files in this app:"
        android:textColor="#CCCCCC"
        android:textSize="13sp"
        android:fontFamily="monospace"
        android:paddingBottom="12dp" />

    <!-- Command Box Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#333333"
        android:padding="1dp">

        <!-- Command TextView -->
        <TextView
            android:id="@+id/setup_command"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:background="#000000"
            android:textColor="#FFFFFF"
            android:fontFamily="monospace"
            android:textSize="11sp"
            android:padding="10dp"
            android:scrollbars="vertical"
            android:textIsSelectable="true" />
    </LinearLayout>

    <!-- Don't Show Again Checkbox -->
    <CheckBox
        android:id="@+id/setup_checkbox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Don't show again"
        android:textColor="#FFFFFF"
        android:buttonTint="#FFFFFF"
        android:fontFamily="monospace"
        android:textSize="13sp"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="16dp" />

    <!-- Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end">

        <Button
            android:id="@+id/btn_copy"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="COPY"
            android:textColor="#000000"
            android:backgroundTint="#FFFFFF"
            android:fontFamily="monospace"
            android:layout_marginEnd="8dp" />

        <Button
            android:id="@+id/btn_close"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="CLOSE"
            android:textColor="#FFFFFF"
            android:backgroundTint="#333333"
            android:fontFamily="monospace" />
    </LinearLayout>
</LinearLayout>
